.artikel-section {
  padding: 60px 0;
  background-color: #fff8f3; /* Warna background mirip seperti gambar */
  font-family: 'Poppins', sans-serif;
  color: #5a2e0f;
}

.breadcrumb {
  font-size: 14px;
  color: #9c5e3b;
  margin-bottom: 10px;
}

.breadcrumb a {
  color: #9c5e3b;
  text-decoration: none;
}

.breadcrumb span {
  font-weight: bold;
}

.artikel-title {
  font-size: 36px;
  font-weight: 700;
  margin-bottom: 30px;
  color: #5a2e0f;
}

.artikel-filter {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
}

.filter-btn {
  padding: 10px 20px;
  border: 2px solid #6e3b1e;
  background-color: transparent;
  color: #6e3b1e;
  border-radius: 30px;
  font-weight: 500;
  cursor: pointer;
  transition: 0.3s;
}

.filter-btn.active,
.filter-btn:hover {
  background-color: #6e3b1e;
  color: white;
}
